# Project Screenshots Folder

Drop your screenshots here:
1. `venue-booking.png` -> Venue Booking Platform UI screenshot
2. `dailydeck.png` -> DailyDeck UI screenshot
3. `bugdeck.png` -> BugDeck UI screenshot

Next.js will automatically display them in the release cards with fallback to clean placeholder wireframes if missing.
